SELECT convert(varchar(10),STARTDATE,20) as STARTDATE,convert(varchar(10),ENDDATE,20) AS ENDDATE,SCHOOL,USERS.TEACHERNO,RECNO,USERNAME
FROM STUDY INNER JOIN USERS ON USERS.TEACHERNO=STUDY.TEACHERNO
WHERE USERS.USERNAME=:USERNAME
ORDER BY STARTDATE