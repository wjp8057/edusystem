SELECT COUNT(REGDATA.STUDENTNO)
FROM REGDATA,STUDENTS
WHERE (REGDATA.YEAR=:YEAR)AND(REGDATA.TERM=:TERM)
AND(REGDATA.STUDENTNO=STUDENTS.STUDENTNO)
AND(STUDENTS.CLASSNO=:CLASSNO)
AND(REGDATA.REGCODE<>'C')AND(REGDATA.REGCODE<>'D')